include /blocks/language-selector/index
include /blocks/screencast-summary/index

mixin screencast-header(data)
  - let {languages} = data;
  
  +b.screencast-header
    +screencast-summary()
    +language-selector({languages})